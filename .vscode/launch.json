{
    "version": "0.2.0",
    "configurations": [
    
        {
            "name": "c++ Launch",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceRoot}/maxChangeNumber.out",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceRoot}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "lldb"
        },
        { 
            "name": "c++ Attach",
            "type": "cppdbg",
            "request": "attach",
            "program": "${workspaceRoot}/maxChangeNumber.out",
            "processId": "${command:pickProcess}",
            "MIMode": "lldb"
        }
    ]
}